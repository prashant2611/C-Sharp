using System;
using System.Collections.Generic;
using System.Text;

namespace Nulls
{
    class MainClass
    {
       /* static void Main(string[] args)
        {
            /*PlayerCharacter pobj = new PlayerCharacter();
            pobj.name = "Prashant";
            pobj.days_since_lastLogin = 10;
            PlayerDisplayer.display(pobj);
            

            PlayerCharacter[] player = new PlayerCharacter[3]
            {
                new PlayerCharacter{name="Prashant"},
                new PlayerCharacter(),
                null
            };

            string p1 = player?[0]?.name;
            string p2 = player?[1]?.name;
            string p3 = player?[2]?.name;

            Console.WriteLine(p1);
            Console.WriteLine(p2);
            Console.WriteLine(p3);
        }
    */
    }
}
